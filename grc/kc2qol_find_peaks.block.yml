# auto-generated by grc.converter

id: kc2qol_find_peaks
label: Find Peaks
category: '[KC2QOL]'

parameters:
-   id: min_dist
    label: Min Dist
    dtype: float
-   id: x_min
    label: X Min
    dtype: float
    default: '0'
-   id: x_max
    label: X Max
    dtype: float
    default: '1'
-   id: vlen
    label: Vector length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   label: out0
    domain: stream
    dtype: float
-   label: out1
    domain: stream
    dtype: float

templates:
    imports: import kc2qol
    make: kc2qol.find_peaks(${min_dist}, ${x_min}, ${x_max}, ${vlen})

file_format: 1
