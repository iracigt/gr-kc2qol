# auto-generated by grc.converter

id: kc2qol_gaussian_blur
label: 1D Gaussian Blur
category: '[KC2QOL]'

parameters:
-   id: m
    label: Number of window points
    dtype: int
-   id: p
    label: Shape
    dtype: float
    default: '0.5'
-   id: sigma
    label: Sigma
    dtype: float
    default: '1'
-   id: vlen
    label: Vector length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

templates:
    imports: import kc2qol
    make: kc2qol.gaussian_blur(${m}, ${p}, ${sigma}, ${vlen})

file_format: 1
