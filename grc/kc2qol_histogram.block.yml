# auto-generated by grc.converter

id: kc2qol_histogram
label: Histogram
category: '[KC2QOL]'

parameters:
-   id: points
    label: Number of points
    dtype: int
-   id: nbins
    label: Number of bins
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: range
    label: Range
    dtype: raw
    default: None
-   id: density
    label: Density Function
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: ['NO', 'YES']

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float
    vlen: ${ nbins }

templates:
    imports: import kc2qol
    make: |-
        kc2qol.histogram(points=${points}, nbins=${nbins}, density=${density}
              % if range:
              , range=${range}
              % endif
              )

file_format: 1
